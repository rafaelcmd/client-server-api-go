version: '3'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.Server
    ports:
      - '8080:8080'

  client:
    build:
      context: .
      dockerfile: Dockerfile.Client
    volumes:
      - ./file:/app/file
    depends_on:
      - server